const result=["banana","apple","Mango", "grapes"];
const result1=['Carrot','Cabbage','Tindora'];

/*result.length=3;
const size=result.length;
console.log(result);

to string method
const value=result.tostring;
console.log(value);

pop- removing last element from array

//result.pop();
//push - to add an element

//result.push();

//shift method - it will remove the first element and shift to next to the previous one at respective position

result.shift();

 unshift method - It will used to add element at first position and shift remaining element to next positions.
 
result.unshift('Orange');


//merge arrays:

const combined = [...result,...result1];
//console.log (combined);

//const=mergedarray=result.concat(result1);

console.log(result.concat(result1));
console.log(result);
splice method
total numbers of param->
first param->index from where you want to insert new element
second param->how many element should be removed
third param-> which element should be added
result.splice(2, 0, "Tomato", "Onion");
*/

const value=result.slice(1,2);
console.log(value);

/* const user={
    name="Ravi"
};
console.log(user.address.city)// syntax error


let x =5;
if(x=10){
    console.log("x is 10");
}// x is 10
*/